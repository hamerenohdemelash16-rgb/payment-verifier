# Excluded to prevent committing Firebase API keys/config publicly
# (see README "Security" section for details on this fix)
google-services.json*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
# NOTE: google-services.json is intentionally NOT ignored here, because
# GitHub Actions needs it to build the APK in the cloud. Keep this repo
# PRIVATE on GitHub so this file isn't publicly visible.
